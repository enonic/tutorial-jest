# Settings for publishing to a Maven repo
group = com.example

# Gradle Project settings
projectName = jest
version = 1.0.0-SNAPSHOT

# XP App values
appName = com.example.jest

# Min required version of XP
xpVersion = 8.0.3
